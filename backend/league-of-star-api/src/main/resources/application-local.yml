spring:
  datasource:
    url: jdbc:mysql://localhost:3306/league_of_star?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: league_of_star
    password: league_of_star1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoint:
    health:
      show-details: always
